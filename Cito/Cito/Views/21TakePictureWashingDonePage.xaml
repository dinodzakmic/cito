<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:Cito.Framework.Controls;assembly=Cito"
             x:Class="Cito.Views.TakePictureWashingDonePage"
             BindingContext="{Binding Source={StaticResource Locator}, Path=TakePictureWashingDone}"
             BackgroundImage="{StaticResource BackgroundGradient}">
    <ContentPage.Content>
        <StackLayout>
            <controls:CitoButton Command="{Binding TakePhotoCommand}" Text="TAKE PHOTO" />
            
            <Image Source="{Binding Photo}"
                   HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand"
                   Aspect="AspectFill"/>
            
            <controls:CitoButton Text="DONE"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>