<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:Cito.Framework.Controls;assembly=Cito"
             x:Class="Cito.Views.UserTypePage">

    <StackLayout Padding="20, 40" Spacing="10">
        <Image Source="{StaticResource LogoImage}" Aspect="AspectFit" />
        <StackLayout HeightRequest="40" />

        <controls:CitoLabel Text="Sign in as" FontSize="10"
                            TextColor="{StaticResource CitoAccent}"
                            HorizontalTextAlignment="Center" />

        <controls:CitoButton Text="CAR OWNER" Margin="15,0" />

        <controls:CitoLabel Text="OR" FontAttributes="Bold" FontSize="15"
                            TextColor="{StaticResource CitoMain}"
                            HorizontalTextAlignment="Center" Margin="0,10" />

        <controls:CitoButton Text="CAR WASHER" Margin="15,0" />
    </StackLayout>
  
</ContentPage>