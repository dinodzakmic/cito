<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:Cito.Framework.Controls;assembly=Cito"
             xmlns:components="clr-namespace:Cito.Framework.Components;assembly=Cito"
             x:Class="Cito.Views.AvailabiltyPage"
             BackgroundImage="{StaticResource BackgroundImage}">

    <StackLayout HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand" Padding="0,80,0,0">

      <controls:CitoLabel Text="WASHER NO. 1"
                          Margin="0,60,0,0"
                          FontSize="18"
                          FontAttributes="Bold"
                          TextColor="{StaticResource CitoMain}"
                          VerticalOptions="CenterAndExpand"
                          VerticalTextAlignment="Center"
                          HorizontalOptions="CenterAndExpand"
                          HorizontalTextAlignment="Center"/>
      
        <StackLayout HorizontalOptions="CenterAndExpand" VerticalOptions="StartAndExpand"
                     Orientation="Horizontal" Spacing="15">

            <controls:CitoLabel Text="ONLINE"
                                FontSize="14"
                                FontAttributes="Bold"
                                TextColor="{StaticResource CitoMain}"
                                VerticalOptions="CenterAndExpand"
                                VerticalTextAlignment="Center"
                                HorizontalOptions="CenterAndExpand"
                                HorizontalTextAlignment="Center"/>


            <components:CitoSwitch IsToggled="False"
                                   VerticalOptions="CenterAndExpand"
                                   HorizontalOptions="CenterAndExpand" />

            <controls:CitoLabel Text="OFFLINE"
                                FontSize="14"
                                FontAttributes="Bold"
                                TextColor="{StaticResource CitoMain}"
                                VerticalOptions="CenterAndExpand"
                                VerticalTextAlignment="Center"
                                HorizontalOptions="CenterAndExpand"
                                HorizontalTextAlignment="Center"/>

        </StackLayout>
    </StackLayout>

</ContentPage>