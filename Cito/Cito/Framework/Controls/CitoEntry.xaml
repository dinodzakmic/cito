<?xml version="1.0" encoding="UTF-8"?>
<Entry xmlns="http://xamarin.com/schemas/2014/forms"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       x:Class="Cito.Framework.Controls.CitoEntry"
       TextColor="{StaticResource CitoLight}"
       PlaceholderColor="{StaticResource CitoLightContrast}"
       FontSize="14"
       BackgroundColor="Transparent"
       TextChanged="NEntry_OnTextChanged"
       Focused="CitoEntryFocused"
       Unfocused="CitoEntryUnfocused">
    <Entry.HeightRequest>
        <OnPlatform x:TypeArguments="x:Double">
            <On Platform="Android" Value="45" />
            <On Platform="iOS" Value="50" />
        </OnPlatform>
    </Entry.HeightRequest>
</Entry>
